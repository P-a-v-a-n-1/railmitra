import 'package:flutter/material.dart';

class SeatAvailabilityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Seat Availability by Source and Destination'),
      ),
      body: Center(
        child: Text('Seat Availability Page'),
      ),
    );
  }
}
